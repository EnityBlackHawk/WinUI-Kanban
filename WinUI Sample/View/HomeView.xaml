<Page
    x:Class="WinUI_Sample.View.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI_Sample.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"
                   FontSize="25" Text="{x:Bind ViewModel.Message}"/>
        <Button Content="Go back" Width="120" Margin="10" VerticalAlignment="Center" HorizontalAlignment="Center"
                CornerRadius="10" Command="{x:Bind ViewModel.ButtonCommand}"/>
    </StackPanel>
</Page>
